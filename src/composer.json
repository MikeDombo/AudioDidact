{
    "require": {
        "pug-php/pug": "@stable",
        "symfony/yaml": "@stable",
        "ext-pdo": "*"
    },
	"require-dev": {
        "phpunit/phpunit": "^9.1.1"
    },
    "suggest": {
        "mongodb/mongodb": "Requires version ^1.1 for use with the MongoDB driver."
    }
}
